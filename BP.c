

int main(void)
{
    // mapping (like lab 3)...

    // offset for GPEDS ...

    // sched_setsched(0, policy, priority);
        // period of 60 ms
    
    //initialize the timer (lab2 pt2)
        // set clock init clock_gettime();

    while(1)
    {
        if( GPEDS IS SET)
        {
            // Create timeStamp
            
            // write to P2 through pipe (/tmp/BP_Pipe)

            // Clear GPEDS
        }


    }
}